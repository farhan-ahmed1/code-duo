[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node /app/apps/server/dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
